для работы данного проекта необходимо сделать import asyncio c помощью pip install asyncio
также сделать:
from aiogram import Bot,Dispatcher,types,Router
from aiogram.filters import Command
from config import TOKEN
